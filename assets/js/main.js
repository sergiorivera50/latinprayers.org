/* latinprayers.org — progressive enhancement only.
 *
 * The site is fully readable and navigable with JavaScript disabled.
 * Nothing essential belongs here. Add only optional niceties
 * (e.g. copy-to-clipboard, client-side search/filter) that degrade gracefully.
 */
(function () {
  "use strict";
  // Intentionally minimal for the skeleton. Enhancements go here.
})();
